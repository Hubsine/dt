{% extends '@FOSUser/layout.html.twig' %}

{% block content_page %}

    {% if users is defined %}

        <div class="row">
            {% for user in users %}
                {% include '@FOSUser/MembersLoop/container.html.twig' %}
            {% endfor %}
        </div>

    {% endif %} 

{% endblock content_page %}
