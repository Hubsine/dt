{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aboutusersreplies list</h1>
    
    {{ aboutUsers }}

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for aboutUsersReply in aboutUsersReplies %}
            <tr>
                <td><a href="{{ path('aboutusersreply_show', { 'id': aboutUsersReply.id }) }}">{{ aboutUsersReply.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('aboutusersreply_show', { 'id': aboutUsersReply.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('aboutusersreply_edit', { 'id': aboutUsersReply.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aboutusersreply_new') }}">Create a new aboutUsersReply</a>
        </li>
    </ul>
{% endblock %}
