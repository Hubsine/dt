{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aboutusersreply</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aboutUsersReply.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aboutusersreply_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('aboutusersreply_edit', { 'id': aboutUsersReply.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
